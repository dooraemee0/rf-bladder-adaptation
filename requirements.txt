# Stage-2 adaptation (DER++ and variants). Python 3.10+ recommended.
# Pin versions to your environment before release; the ranges below are known-good.
torch>=2.0
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7
pyyaml>=6.0
